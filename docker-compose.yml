services:

  backend:
    build: .

    container_name: ai-server-agent-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./credentials:/app/credentials

    restart: unless-stopped